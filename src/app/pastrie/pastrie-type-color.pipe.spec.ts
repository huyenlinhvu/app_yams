import { PastrieTypeColorPipe } from './pastrie-type-color.pipe';

describe('PastrieTypeColorPipe', () => {
  it('create an instance', () => {
    const pipe = new PastrieTypeColorPipe();
    expect(pipe).toBeTruthy();
  });
});
